function im_prob=imm_prob(leav,tList,mu,lamda)
    
    if ~isempty(leav)
        im_prob=1;
        for i=1:size(leav,1)
            t=tList(i)
            n=sum(leav(i,:)~='-')
            x=p_nddt(mu,lamda,t,n);
            im_prob=im_prob*x;
        end
    else
        

end